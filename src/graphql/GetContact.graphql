query GetContact($id: ID!) {
  contact(id: $id) {
    id
    fullName
    category
    createdAt
    phoneNumber
    survey {
      id
      name
      answers {
        answer
        question
      }
    }
  }
}
