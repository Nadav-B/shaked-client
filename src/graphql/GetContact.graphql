query GetContact($where: ContactUniqueInput!) {
  contact(where: $where) {
    id
    fullName
    category
    createdAt
    phoneNumber
    survey {
      id
      name
      answers {
        answer
        question
      }
    }
  }
}
