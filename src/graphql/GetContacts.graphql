query GetContacts {
    contacts {
        id
        fullName
        category
        createdAt
        phoneNumber
        survey {
            id
            name
            answers {
                answer
                question
            }
        }
    }
}
