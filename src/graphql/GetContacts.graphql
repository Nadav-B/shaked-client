query GetContacts {
    contacts {
        id
        fullName
        category
        phoneNumber
        survey {
            id
            name
            answers {
                answer
                question
            }
        }
    }
}
