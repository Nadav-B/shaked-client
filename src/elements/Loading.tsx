import { RotateCircleLoading } from "react-loadingg";
import Flex from "./Flex";

const Loading = () => {
  return (
    <Flex>
      <RotateCircleLoading color="#21646b" />
    </Flex>
  );
};

export default Loading;
